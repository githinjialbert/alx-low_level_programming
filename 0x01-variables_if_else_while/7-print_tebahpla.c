#include <stdio.h>
/**
*main - Entry point
*Description: Smile in the mirror
*Return: always 0
*/
int main(void)
{
	int w = 122;

	while (w >= 97)
	{
		putchar(w);
		w--;
	}
	putchar('\n');
return (0);
}
