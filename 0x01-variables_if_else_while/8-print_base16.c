#include <stdio.h>
/**
*main - Entry point
*Description:Hexadecimal
*Return: always 0
*/
int main(void)
{
	int w;
	int r;

	for (w = 48; w <= 48; w++)
	{
		putchar(w);
		w++;
	}
	for (r = 97; r <= 102; r++)
	{
		putchar(r);
		r++;
	}
	putchar('\n');
	return (0);
}
