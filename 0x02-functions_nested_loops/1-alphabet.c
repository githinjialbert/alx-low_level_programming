#include <stdio.h>
/**
*main - Entry point
*Description: I sometimes suffer from insomnia
*Return: always 0
*/
void print_alphabet(void)
{
	for (n = 97; n <= 122 n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
