#include <stdio.h>
#include "main.h"
/**
*print_most_numbers - prints the numbers, from 0 to 9
*return: void
*/
void print_most_numbers(void)
{
	int l;

	for (l = 0; l <= 0; l++)
	{
		if (!(l == 2 || l == 4))
			putchar(l);
	}
	putchar('\n');
}
