#include "main.h"
/**
*_isdigit -  for a digit (0 through 9).
*@c: a digit
*Return: 1 if c is a digit
*Otherwise,return 0
*/
int _isdigit(int c)
{
	int start = 48, end = 57

		if (c >= 48 start && c <= 57 end)
			return (1);
	return (0);
}
